# neo-wawo 私有配置模板

> 复制成仓库外私有目录里的 `private-config.md`（已软链入 memory/，被 .gitignore 屏蔽）。
> **这里写你的个人路径，绝不入库、绝不上传。**

## 经历卡片库（运行时唯一素材源，由 /wawo-learn 蒸馏生成）
- **路径**：`<填你的私有卡片库目录绝对路径>`（repo 内软链 `memory/cards/`）
- **内容**：`01-厚卡.md`、`02-薄卡-需回采.md`、`03-原话索引.md`（格式见 `learn.md`）
- **权限**：读写。运行时只读这里挖选题；回采到真实故事时按 `learn.md` 升卡规则写回。

## 原始经历库（蒸馏原料——仅 /wawo-learn 时读，运行时不读）
- **路径**：`<填你的人生总览/笔记库/对话导出绝对路径>`
- **读取范围**：`<列出要读的文件/领域，或写「全部」>`
- **权限**：只读。仅 `/wawo-learn` 蒸馏时读。绝不复制进 repo、绝不上传、绝不写回。

## 隐私 / 公开度
- wawo 只用经历库挖选题、找角度、提意见，**绝不代笔、绝不把库内容复制/上传/写回**。
- 敏感数字、私密领域（健康/心理/生活等）：做选题和提意见时点到为止；是否写进、写多细、发不发，全由用户自己定——wawo 只提醒，不替他决定。
